#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * main - Prints the alphabet without q and e.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 9; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
